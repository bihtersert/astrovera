
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Geçici olarak herkese izin veriyoruz (Test bittikten sonra burayı kapatacağız)
      allow read, write: if true;
    }
  }
}
